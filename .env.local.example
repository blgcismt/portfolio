# Supabase — create a free project at https://supabase.com
# Project Settings → API → copy these two values
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Then run this SQL in the Supabase SQL Editor:
#
# create table snake_scores (
#   id uuid primary key default gen_random_uuid(),
#   username text not null check (char_length(username) <= 20),
#   score integer not null check (score >= 0),
#   created_at timestamptz default now()
# );
#
# create table wordle_streaks (
#   id uuid primary key default gen_random_uuid(),
#   username text not null check (char_length(username) <= 20),
#   streak integer not null check (streak >= 1),
#   lang text not null default 'en',
#   created_at timestamptz default now()
# );
#
# alter table snake_scores enable row level security;
# alter table wordle_streaks enable row level security;
#
# create policy "public read" on snake_scores for select using (true);
# create policy "public insert" on snake_scores for insert with check (true);
# create policy "public read" on wordle_streaks for select using (true);
# create policy "public insert" on wordle_streaks for insert with check (true);
